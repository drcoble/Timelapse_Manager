# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Generated release artifacts (produced by `make release`/`sbom`/`sign`)
cyclonedx-bom.json
SHA256SUMS
SHA256SUMS.asc

# Generated build metadata (commit SHA + build date), written by the release
# script at build time and read back through version.py. Never committed; a
# dev checkout simply has no build info.
src/timelapse_manager/_build_info.py

# Virtual environments
.venv/
venv/
env/

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Local runtime / secrets / data (never commit)
.env
*.local
data/
.local-token
.secret-key
*.db
*.db-wal
*.db-shm
*.sqlite3
*.sqlite3-*
certs/
*.pem
*.key

# Local development TLS certificate (generated, never commit)
.dev-cert.pem
.dev-cert-key.pem

# OS / editor / tooling
.DS_Store
.idea/
.vscode/
.claude/

# Rendered Linux-package build artifacts (nfpm)
packaging/build/
